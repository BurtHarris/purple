const vscode = require('vscode');

function activate(context) {
  // Color picker feature removed. Extension activates with no UI command.
}

function deactivate() {
  // no-op
}

module.exports = { activate, deactivate };
